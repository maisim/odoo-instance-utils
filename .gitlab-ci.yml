default:
  image: python:3.12-slim

stages:
  - test

test:
  stage: test
  before_script:
    - pip install uv --quiet
    - uv sync --group dev
  script:
    - uv run pytest tests/ -v
  only:
    - main
